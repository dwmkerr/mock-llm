{{- if .Values.ark.a2a.enabled -}}
apiVersion: ark.mckinsey.com/v1prealpha1
kind: A2AServer
metadata:
  name: {{ include "mock-llm.fullname" . }}-message-counter
  labels:
    {{- include "mock-llm.labels" . | nindent 4 }}
spec:
  address:
    {{- if and .Values.ark.a2a.agentCardHost .Values.ark.a2a.agentCardPort }}
    value: http://{{ .Values.ark.a2a.agentCardHost }}:{{ .Values.ark.a2a.agentCardPort }}/a2a/agents/message-counter-agent
    {{- else }}
    valueFrom:
      serviceRef:
        name: {{ include "mock-llm.fullname" . }}
        port: http
        path: /a2a/agents/message-counter-agent
    {{- end }}
  description: Counts messages received per conversation context
  pollInterval: {{ .Values.ark.a2a.pollInterval | default "3s" }}
{{- end }}
