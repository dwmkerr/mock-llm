apiVersion: v1
kind: Service
metadata:
  name: {{ include "mock-llm.fullname" . }}
  labels:
    {{- include "mock-llm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mock-llm.selectorLabels" . | nindent 4 }}
